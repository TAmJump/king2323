CREATE TABLE IF NOT EXISTS quiz_questions (id INTEGER PRIMARY KEY AUTOINCREMENT, group_id INTEGER NOT NULL, language TEXT NOT NULL DEFAULT 'ja', category TEXT, difficulty INTEGER NOT NULL DEFAULT 2, question TEXT NOT NULL, choices_json TEXT NOT NULL, correct_index INTEGER NOT NULL, explanation TEXT, active INTEGER NOT NULL DEFAULT 1, created_at TEXT NOT NULL DEFAULT (datetime('now')), UNIQUE (group_id, language));
